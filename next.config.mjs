/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placehold.co', 'clatwallah.netlify.app', 'cdn.pixabay.com', 'www.istockphoto.com'],
  },
};

export default nextConfig;
